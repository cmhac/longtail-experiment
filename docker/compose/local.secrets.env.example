# Copy to docker/compose/.env.local for local development secrets.
# This example file is tracked, but docker/compose/.env.local is gitignored.

FRED_API_KEY=<replace-with-your-real-fred-api-keys>
DAGSTER_METADATA_DB_PASSWORD=<replace-with-your-dagster-metadata-db-password>
