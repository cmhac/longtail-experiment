# longtail-experiment Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-23

## Active Technologies

- Python 3.12 (backend); TypeScript 5.x frontend unchanged + FastAPI 0.115.x, uvicorn[standard], SQLAlchemy 2.x (already in backend), psycopg 3.x (already in backend), Pydantic 2.x (already in backend), structlog (already in backend), pytest + httpx (test client), Nx tooling (014-read-only-fastapi-api)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.12 (backend); TypeScript 5.x frontend unchanged: Follow standard conventions

## Recent Changes

- 014-read-only-fastapi-api: Added Python 3.12 (backend); TypeScript 5.x frontend unchanged + FastAPI 0.115.x, uvicorn[standard], SQLAlchemy 2.x (already in backend), psycopg 3.x (already in backend), Pydantic 2.x (already in backend), structlog (already in backend), pytest + httpx (test client), Nx tooling

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
